{
  "framework": "angular",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/medicineFaculty",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

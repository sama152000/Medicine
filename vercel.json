{
  "framework": "angular",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/medicineFaculty/browser",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
